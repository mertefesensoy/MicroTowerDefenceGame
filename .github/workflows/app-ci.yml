name: iOS App CI (DISABLED - Migrated to Expo)

# Temporarily disabled while migrating to Expo
# See strategy-c/expo-migration branch for the new React Native implementation

on:
  workflow_dispatch:  # Only run manually

jobs:
  build:
    name: Build iOS App (Swift - Legacy)
    runs-on: macos-14
    if: false  # Disabled
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: This workflow is disabled
        run: echo "Swift CI disabled - using Expo now"
