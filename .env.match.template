# Secrets for Fastlane Match
# DO NOT COMMIT THIS FILE

# Match Repo (Private - HTTPS)
MATCH_GIT_URL="https://github.com/mertefesensoy/microtd-match.git"

# Basic Auth Header (Base64 of username:token)
# Generate: printf "mertefesensoy:TOKEN" | base64 | tr -d '\n'
MATCH_GIT_BASIC_AUTHORIZATION="YOUR_BASE64_HEADER"

# Password to encrypt/decrypt the certificates in the git repo
MATCH_PASSWORD="CHANGE_ME_TO_A_STRONG_PASSWORD"

# Apple Developer Team ID (Q885KHD85V)
DEVELOPER_PORTAL_TEAM_ID="Q885KHD85V"

# App Store Connect API Key (for TestFlight upload lane)
# Generate Base64: base64 < AuthKey_XXXX.p8 | tr -d '\n' | pbcopy
ASC_KEY_ID="YOUR_KEY_ID"
ASC_ISSUER_ID="YOUR_ISSUER_ID"
ASC_KEY_P8_B64="YOUR_BASE64_P8_CONTENT"
