<<<<<<< HEAD
# Match Configuration Template
# Copy this file to .env.match and fill in your actual values
# DO NOT commit .env.match to git! (Already in .gitignore)
#
# Usage: Load with 'set -a; source .env.match; set +a' before running fastlane

# ============================================================================
# REQUIRED: Fill these in with your actual values
# ============================================================================

MATCH_GIT_URL=""
MATCH_PASSWORD=""

APPLE_ID=""
FASTLANE_TEAM_ID=""
DEVELOPER_PORTAL_TEAM_ID=""
APP_STORE_CONNECT_TEAM_ID=""

# Note: If you only know one Team ID, set all three to the same value.

# ============================================================================
# AUTH OPTIONS: Choose one and fill in MATCH_GIT_URL above
# ============================================================================

# Option 1A: HTTPS with embedded token (convenient but logs may leak token)
# WARNING: Special characters in token may break URL parsing
# MATCH_GIT_URL="https://YOUR_GITHUB_TOKEN@github.com/yourusername/microtd-match.git"

# Option 1B: HTTPS without token in URL (safer)
# MATCH_GIT_URL="https://github.com/yourusername/microtd-match.git"
# MATCH_GIT_BASIC_AUTHORIZATION="BASE64(username:token)"

# Option 2: SSH (recommended if SSH key is set up)
# MATCH_GIT_URL="git@github.com:yourusername/microtd-match.git"
=======
# Secrets for Fastlane Match
# DO NOT COMMIT THIS FILE

# Git URL for the private match repo
MATCH_GIT_URL="git@github.com:USERNAME/microtd-match.git"
# OR https://... if using token

# Password to encrypt/decrypt the certificates in the git repo
MATCH_PASSWORD="CHANGE_ME_TO_A_STRONG_PASSWORD"

# Apple Developer Team ID (from developer.apple.com)
DEVELOPER_PORTAL_TEAM_ID="ABC123XYZ"
>>>>>>> 4e34510 (feat: setup release automation pipeline (fastlane + gha))
